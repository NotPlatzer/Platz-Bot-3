const Discord = require("discord.js");

module.exports = {
    name: "kick",
    aliases: ['pi'],
    cooldown: 1000 * 5,
    description: "Kicks a member",


    async run(client, message, args) {
        
        if(1 === 1) return;

    }
    
}